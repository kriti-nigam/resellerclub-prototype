Inline code token for endpoints, params, and identifiers inside body copy.

```jsx
<p>Send a <Code>POST</Code> to <Code>/domains/register</Code>.</p>
```

On light surfaces the text is accent-700. Pass `onDark` inside ink/code panels.
