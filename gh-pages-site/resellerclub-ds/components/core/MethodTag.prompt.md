Monospace HTTP-method chip for API docs / endpoint rows.

```jsx
<MethodTag method="POST" />
<MethodTag method="GET" size="sm" />
```

Colors are fixed per verb: GET=teal, POST=violet, PUT/PATCH=amber, DELETE=red. Use `sm` inside dense lists (command palette, sidebars).
